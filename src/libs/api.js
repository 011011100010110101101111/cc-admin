import utils from './util.js';const qs = {    stringify: function (data) {        if (typeof (data) === 'undefined' || data === null || typeof (data) !== 'object') {            return '';        }        var url = '';        for (var k in data) {            url += ((url.indexOf('=') !== -1) ? '&' : '') + k + '=' + encodeURI(data[k]);        }        return url;    }};export const requestLogin = params => { return utils.ajax.post('user/list', qs.stringify(params)).then(res => res.data);};